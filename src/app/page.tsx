/**
 * Main Page Component
 * Displays the GitHub repository viewer
 */

import RepositoryList from '@/app/components/RepositoryList';

const Page = () => {
    return <RepositoryList />;
};

export default Page;
